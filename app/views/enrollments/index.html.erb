<h1>Enrollments</h1>
<table class="table table-striped">
  <thead><tr><th>Student</th><th>Course</th></tr></thead>
  <tbody>
    <% Enrollment.all.each do |e| %>

      <tr><td><%= e.student.user.email %></td><td><%= e.course.title %></td></tr>
    <% end %>
  </tbody>
</table>
